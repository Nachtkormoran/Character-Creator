/**
 * Zentrale Icon-Quelle (Lucide). Ein Ort für das ganze Icon-Set – so lässt sich
 * die Bibliothek an genau einer Stelle tauschen, und die Größe/Strichstärke
 * bleibt über `ICON_SIZE`/`ICON_STROKE` konsistent (statt Emojis als Icons).
 */
export {
  Plus,
  Upload,
  Download,
  Search,
  X,
  Pencil,
  Trash2,
  Copy,
  Star,
  Sparkles,
  Dices,
  BookOpen,
  Image as ImageIcon,
  Images,
  Library,
  User,
  Users,
  FileText,
  Settings,
  Sun,
  Moon,
  Monitor,
  ChevronRight,
  ChevronUp,
  ChevronDown,
  Loader2,
  AlertTriangle,
  CheckCircle2,
  Check,
  Mountain,
  ListTree,
  BookText,
} from "lucide-react";

/** Einheitliche Icon-Größe (px) und Strichstärke fürs ganze UI. */
export const ICON_SIZE = 18;
export const ICON_STROKE = 1.75;
